/**
 * Technical runtime-configuration registry.
 * Business modules wrap this JSON persistence behind typed policy repositories;
 * they must not turn domain invariants into arbitrary runtime settings.
 */
package com.lyreo.platform.config;
