// Public surface of the design system.
// primitiveColors is intentionally NOT re-exported; feature code must use semantic roles.
export { lyreoBrand, spacing, radius, motion, typography } from './primitives';
export * from './semantic';
